name: 构建 Caddy 插件版本

on:
  workflow_dispatch:
    inputs:
      version:
        description: '发布版本号 (例如: v2.10.0)'
        required: true
        type: string
        default: 'v2.10.0'
      plugins:
        description: '插件列表 (用英文逗号分隔)'
        required: true
        type: string
        default: 'github.com/caddyserver/caddy/v2,github.com/mholt/caddy-ratelimit,github.com/lanrat/caddy-dynamic-remoteip,github.com/greenpau/caddy-security,github.com/caddy-dns/cloudflare'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            suffix: linux-amd64
          - goos: linux
            goarch: arm64
            suffix: linux-arm64
          - goos: windows
            goarch: amd64
            suffix: windows-amd64
            ext: .exe
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置 Go 环境
      uses: actions/setup-go@v5
      with:
        go-version: 'stable'
        
    - name: 安装 xcaddy
      run: go install github.com/caddyserver/xcaddy/cmd/xcaddy@latest
      
    - name: 准备插件参数
      id: prepare
      run: |
        plugins="${{ inputs.plugins }}"
        with_args=""
        IFS=',' read -ra PLUGIN_ARRAY <<< "$plugins"
        for plugin in "${PLUGIN_ARRAY[@]}"; do
          plugin=$(echo "$plugin" | xargs)  # 去除空格
          if [ -n "$plugin" ]; then
            with_args="$with_args --with $plugin"
          fi
        done
        echo "with_args=$with_args" >> $GITHUB_OUTPUT
        
    - name: 构建 Caddy (${{ matrix.suffix }})
      run: |
        xcaddy build ${{ steps.prepare.outputs.with_args }}
      env:
        GOOS: ${{ matrix.goos }}
        GOARCH: ${{ matrix.goarch }}
        
    - name: 重命名并检查文件
      run: |
        if [ "${{ matrix.goos }}" = "windows" ]; then
          if [ -f "caddy.exe" ]; then
            mv caddy.exe caddy-${{ matrix.suffix }}.exe
          elif [ -f "caddy" ]; then
            mv caddy caddy-${{ matrix.suffix }}.exe
          else
            echo "错误: 找不到构建的二进制文件"
            ls -la
            exit 1
          fi
        else
          if [ -f "caddy" ]; then
            mv caddy caddy-${{ matrix.suffix }}
          else
            echo "错误: 找不到构建的二进制文件"
            ls -la
            exit 1
          fi
        fi
        
    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: release-binaries
        path: |
          caddy-linux-amd64
          caddy-linux-arm64
          caddy-windows-amd64.exe

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: 下载所有构建产物
      uses: actions/download-artifact@v4
      with:
        name: release-binaries
        path: .
      
    - name: 准备发布说明
      id: release_notes
      run: |
        plugins="${{ inputs.plugins }}"
        echo "## 🚀 Caddy 自定义构建版本" > release_notes.md
        echo "" >> release_notes.md
        echo "### 📦 包含的插件:" >> release_notes.md
        IFS=',' read -ra PLUGIN_ARRAY <<< "$plugins"
        for plugin in "${PLUGIN_ARRAY[@]}"; do
          plugin=$(echo "$plugin" | xargs)  # 去除空格
          if [ -n "$plugin" ]; then
            echo "- \`$plugin\`" >> release_notes.md
          fi
        done
        echo "" >> release_notes.md
        echo "### 💻 支持的架构:" >> release_notes.md
        echo "- Linux AMD64" >> release_notes.md
        echo "- Linux ARM64" >> release_notes.md
        echo "- Windows AMD64" >> release_notes.md
        echo "" >> release_notes.md
        echo "### 📥 下载说明:" >> release_notes.md
        echo "请根据您的系统架构选择对应的二进制文件下载。下载的就是可直接执行的二进制文件。" >> release_notes.md
        
    - name: 创建发布版本
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ inputs.version }}
        name: caddy ${{ inputs.version }}
        body_path: release_notes.md
        files: |
          caddy-linux-amd64
          caddy-linux-arm64
          caddy-windows-amd64.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
